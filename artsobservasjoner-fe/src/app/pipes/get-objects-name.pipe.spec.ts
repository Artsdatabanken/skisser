import { GetObjectsNamePipe } from './get-objects-name.pipe';

describe('GetObjectsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetObjectsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
