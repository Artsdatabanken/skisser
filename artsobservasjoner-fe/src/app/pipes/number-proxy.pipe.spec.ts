import { NumberProxyPipe } from './number-proxy.pipe';

describe('NumberProxyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberProxyPipe();
    expect(pipe).toBeTruthy();
  });
});
