import { NavigateToPageDirective } from './navigate-to-page.directive';

describe('NavigateToPageDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateToPageDirective();
    expect(directive).toBeTruthy();
  });
});
